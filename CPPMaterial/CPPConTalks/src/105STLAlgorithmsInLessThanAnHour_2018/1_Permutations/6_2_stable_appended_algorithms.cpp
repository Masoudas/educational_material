/**
* Whenever stable comes before the name of an algorithm, it's an stable algorithm (like stable sort, stable
* partition).
* 
* A stable algorithm has the property that the relative order keys is not changed.
*/