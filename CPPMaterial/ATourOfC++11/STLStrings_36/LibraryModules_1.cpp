/**
 * The library offers support for character classifications (a set of macros for classifying characters, like 
 * islower(), isupper()) in <cctype> header,
 * support for strings in <string> and regex in <regex> and c-style strings in <cstring>.
 */
#include <cctype>