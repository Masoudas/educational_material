/**
 * Question is, can we define class members const? Yes we can. However, we can't modify them
 * using constructors, or any other function for that matter. 
 * This is because memory space is declared with class memory instantiation.
 * Hence, it can't be modified with constructors.
 */