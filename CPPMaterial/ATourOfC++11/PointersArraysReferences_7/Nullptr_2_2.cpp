/**
 * nullptr is used when a pointer does not refer to an object. In the past,
 * it was 0 (defining a macro such as #define NULL 0 was common). This is because no
 * object is allocated with address zero.
 */