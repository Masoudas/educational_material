/**
* As we can see in the previous example, default operations of a class are not templated. Hence, if
* we want to have a templated version, we define a templated version. The point is that regardless of our
* definition, the default non-template version will still be defined. 
*/