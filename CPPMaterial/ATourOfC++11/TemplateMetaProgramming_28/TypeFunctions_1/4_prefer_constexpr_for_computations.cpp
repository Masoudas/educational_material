/**
* One thing that we should not forget is that for compile-time computations, we should opt for constexpr functions rather
* than type functions. Type functions are supposed to return types, and not values (compile-time values for that matter).
*/