/**
 * As they said, qt contains all sorts of containers. We have for example QPair, QVector, QList, QMap, etc.
 * Apparently, it's better to use the variations of containers inside Q than the standard versions.
 * 
 * For example here, we used QPair to send the two arguments to the compute method.
 */