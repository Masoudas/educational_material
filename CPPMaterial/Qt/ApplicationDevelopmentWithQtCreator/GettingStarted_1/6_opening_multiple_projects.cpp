/**
 * Note that it's possible to open multiple projects in Qt. The one thing I don't understand is how to open
 * the projects. Apparently, we need to select all the project files to open them. Hence for example, it's
 * not sufficient to just click on the console project's .pro file to just open that one.
 */