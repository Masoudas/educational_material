A simple search pattern is like that of vim:

$ awk '/pattern/{print}' file

In that sense, awk can replace the famous grep command.

We can also for example look for patterns that are not there:

$ awk '/!pattern/{print}' file