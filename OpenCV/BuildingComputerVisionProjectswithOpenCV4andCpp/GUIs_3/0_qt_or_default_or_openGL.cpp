/**
 * As a reminder, note that the GUI backend used creating the window, adding buttons, etc, depends on the choice
 * of window when making namedWindow. Hence, if I use expanded, then Qt will be used, etc.
 */