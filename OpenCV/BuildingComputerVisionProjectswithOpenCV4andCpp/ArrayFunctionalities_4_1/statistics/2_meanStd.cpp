/**
 * As it turns out, we can calcuate the mean and std for each channel using the following method:
 * 
 * void cv::meanStdDev(InputArray src, OutputArray mean, OutputArray stddev, InputArray mask = noArray())
 * 
 * So in essense, this function does one additional task compared to mean
 */