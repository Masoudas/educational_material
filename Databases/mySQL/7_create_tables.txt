Once a database is selected, to see the tables:
mySQL> SHOW TABLES;

Once a database is selected, use the famous CREATE command to create a table:

mysql> CREATE TABLE pet (name VARCHAR(20), owner VARCHAR(20),
       species VARCHAR(20), sex CHAR(1), birth DATE, death DATE);

