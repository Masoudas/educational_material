/* Me: I don't get it. But to make dynamically sized arrays, we use make. The capacity is the third argument, which can be passed. Notice that each subarray can have different length.
 */
package main

import "fmt"

func main() {
	v := [][]int{
		[]int{1},
		[]int{1, 2},
	}

	fmt.Println(v)
}
