/*
The init and post statements are optional. Similarly, we can use the for loop as while. Notice also the while true case.
*/

package main

import "fmt"

func main() {
	sum := 1
	for ; sum < 1000; {
    	sum += sum
	}
	fmt.Println(sum)

    for sum < 1000 {
		sum += sum
	}
	fmt.Println(sum)

    for {
        
    }
}