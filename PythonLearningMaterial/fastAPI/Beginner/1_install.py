"""
The test server comes built in and can be installed as follows:

$ pip install fastapi[all]

Otherwise, we can drop the uvicorn server:

$ pip install fastapi
$ pip install uvicorn[standard]

Use the following to install forms:
$ pip install python-multipart.

Use python-pose for jwt authentication:
$ pip install "python-jose[cryptography]"

Use passlib for authentication:
$ pip install "passlib[bcrypt]"
"""
