"""
Use poetry init to initialize poetry:

$ poetry init

The interactive mode allows you to set the proper settings.

Don't forget that there's also:

$ poetry shell

which opens a shell with this environment.
"""
